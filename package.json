{
  "name": "minimalism-defined",
  "version": "1.0.0",
  "description": "Beautifully Simple Websites on Nuxt.js",
  "author": "Dustin Boling",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "build:gh-pages": "DEPLOY_ENV=GH_PAGES nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "generate:gh-pages": "DEPLOY_ENV=GH_PAGES nuxt generate",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup"
  },
  "dependencies": {
    "nuxt": "^2.14.1"
  },
  "devDependencies": {
    "node-sass": "^7.0.0",
    "nodemon": "^1.19.4",
    "push-dir": "^0.4.1",
    "sass-loader": "^7.3.1",
    "tailwindcss": "^1.6.2"
  }
}
